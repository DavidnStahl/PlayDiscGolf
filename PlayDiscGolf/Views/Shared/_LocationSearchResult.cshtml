@model List<PlayDiscGolf.Models.ViewModels.SearchLocationItemViewModel>

@{
    Layout = null;
}




@if (Model.Count > 0)
{
    <ul>
        @foreach (var item in Model)
        {
            <li>@item.Name</li>
        }
    </ul>
}